#include<stdio.h>

int main() {
    int height = 7;
    int width = 5;

    printf("Perimeter of the Rectangle is %d\n\n",2 * (height + width));
    printf("Area of the Rectangle is %d\n\n", height * width);
    return 0;
}